# similar to lists but they are immutable. They can't be changed

numbers = (1, 2, 3, 4)

#UNPACKING a tuple or list, setting each elemnt as a separate variable

# x = numbers[0]
# y = numbers[1]
# z = numbers[2]

x, y, z = numbers
